FROM node:20-alpine

WORKDIR /app
COPY package.json ./
COPY bin ./bin
COPY src ./src

ENV NODE_ENV=production \
    OCQ_GATEWAY_HOST=0.0.0.0 \
    OCQ_GATEWAY_PORT=8088

EXPOSE 8088
CMD ["node", "bin/ocq", "serve"]
